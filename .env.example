# Graph App — environment variables (optional, all have defaults)
# Copy this file to `.env` and customize

# Backend
GRAPH_APP_HOST=127.0.0.1
GRAPH_APP_PORT=8765

# Frontend
# Default: Vite dev server runs on 5173

# Database location
# Default: ./data/app.db (relative to repo root)
